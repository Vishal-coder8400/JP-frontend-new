import React from "react";

const Navbar = () => {
  return (
    <nav className="w-full bg-white border-b border-gray-200">
      {/* Navbar content for superAdmin if needed */}
    </nav>
  );
};

export default Navbar;
